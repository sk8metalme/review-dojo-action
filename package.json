{
  "name": "review-dojo-action",
  "version": "1.0.0",
  "description": "GitHub Action for review-dojo knowledge collection",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sk8metalme/review-dojo-action.git"
  },
  "author": "sk8metalme",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.9.3",
    "esbuild": "^0.27.2",
    "@types/node": "^25.0.3"
  }
}
